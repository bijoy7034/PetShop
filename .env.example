# Root compose env — used only by docker-compose.yml to wire Mongo credentials.
# Backend runtime settings live in backend/.env
MONGO_ROOT_USER=petshop_admin
MONGO_ROOT_PASSWORD=change-me-strong-random
DB_NAME=petshop
